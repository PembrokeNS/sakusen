# Include overall configuration
include $(top_srcdir)config.mk

# Add optimisation flags and turn off debugging info
BUILD_CXXFLAGS := $(BUILD_CXXFLAGS) -O3 -g0
BUILD_CFLAGS := $(BUILD_CFLAGS) -O3 -g0

